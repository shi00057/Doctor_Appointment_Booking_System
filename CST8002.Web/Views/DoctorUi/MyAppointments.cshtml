@model CST8002.Web.Models.MyAppointmentsVm
@{
    ViewData["Title"] = "My Appointments";
}
<h2>My Appointments</h2>
<form method="get" asp-action="MyAppointments">
    <input type="hidden" name="doctorId" value="@Model.DoctorId" />
    <label>From</label>
    <input type="date" name="from" value="@Model.From.ToString("yyyy-MM-dd")" />
    <label>To</label>
    <input type="date" name="to" value="@Model.To.ToString("yyyy-MM-dd")" />
    <button type="submit">Filter</button>
</form>
<table>
    <thead>
        <tr>
            <th>AppointmentId</th>
            <th>PatientId</th>
            <th>Start</th>
            <th>End</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var a in Model.Items)
        {
            <tr>
                <td>@a.ApptId</td>
                <td>@a.PatientId</td>
                <td>@a.StartUtc.ToLocalTime()</td>
                <td>@a.EndUtc.ToLocalTime()</td>
                <td>@a.Status</td>
                <td>
                    <form asp-action="CancelAppointment" method="post">
                        <input type="hidden" name="apptId" value="@a.ApptId" />
                        <input type="hidden" name="doctorId" value="@Model.DoctorId" />
                        <input type="hidden" name="reason" value="Cancelled by doctor" />
                        <button type="submit">Cancel</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    <a asp-action="Schedule" asp-route-doctorId="@Model.DoctorId">Generate Slots</a> |
    <a asp-action="Profile" asp-route-doctorId="@Model.DoctorId">My Profile</a>
</div>
